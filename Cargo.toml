[package]
name = "instruction_bundler"
version = "0.1.0"
edition = "2021"

[features]
no-entrypoint = []

[dependencies]
solana-program = "1.16"

[lib]
name = "instruction_bundler"
crate-type = ["cdylib", "lib"]
doctest = false
